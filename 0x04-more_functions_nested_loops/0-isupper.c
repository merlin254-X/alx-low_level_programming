#include "main.h"

/**
 * _isupper - function that checks for uppercase character
 * @c: The parameter to be checked
 * Return: i if uppercase and 0 if lowercase
 *
 */

int _isupper(int c)
{

if (c >= 'A' && c <= 'Z')

return (1);

else

return (0);
}
