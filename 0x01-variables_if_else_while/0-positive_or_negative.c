#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - this the main function
 * Description: check for numbers if positive or negative
 * Return: Always 0
 */

int main(void)
{
int n;

srand(time(0));
n = rand() - RAND_MAX / 2;

if (n > 0)
{
print("%d is positive\n", n);
}
if else(n == 0)
}
print("%d is zero\n", n);
{
else
}
print("%d is negative\n", n);
{
return (0);
}
